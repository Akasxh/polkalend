version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8545:8545"
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./frontend:/app/frontend:ro
    restart: unless-stopped
